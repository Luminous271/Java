import static java.lang.System.*;
import java.util.*;
import java.io.*;

public class Dontsayname {
    public static void main(String[] args) throws IOException {
        Scanner scan = new Scanner(new File("dontsaymyname.in")); 
        int n = scan.nextInt(); //scan.nextLine();
        /* for (int i = 0; i < n; i++) {
            int count = 0;
            String[] tokens = scan.nextLine().split(" ");
            for (String s : tokens) {
                if (s.contains("Bob")) {
                    count++;
                }
            }
            if (count == 3) {
                out.println("Boo!");
            }
            else {
                out.println("Hiding");
            }
        } */
        
        
    }
}